# Managed by OpenChain edge protection. Do not edit on the host.
# The key is empty outside the OpenChain API/RPC routes; see http_top.conf.
limit_conn openchain_api_connections 10;
limit_conn_status 429;
limit_req zone=openchain_api_rate burst=10 nodelay;
limit_req_status 429;
